name: prediksi_suhu_env
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
      - numpy
      - pandas
      - scikit-learn
      - mlflow==2.14.0
      - matplotlib
      - seaborn
